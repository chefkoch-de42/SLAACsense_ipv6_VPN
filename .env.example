# Docker Image Configuration
# Override the default Docker image if needed
#DOCKER_IMAGE=ghcr.io/notherealmarco/slaacsense:latest

# OPNsense Configuration
OPNSENSE_URL=http://192.168.1.1
OPNSENSE_API_KEY=your_api_key_here
OPNSENSE_API_SECRET=your_api_secret_here

# Technitium DNS Configuration
TECHNITIUM_URL=http://dns.example.com
TECHNITIUM_TOKEN=your_token_here

# General Settings
# DNS_ZONE_SUBNETS supports two formats:
# New format (recommended): zone=subnet1,subnet2;zone2=subnet3
#   DNS_ZONE_SUBNETS=home.example.com=192.168.10.0/24,fd00::/48;office.example.com=192.168.20.0/24,fd01::/48
# Legacy format (still supported): subnet=zone,subnet2=zone2
DNS_ZONE_SUBNETS=192.168.1.0/24=lan.home.arpa
#DO_V4=false
#IGNORE_LINK_LOCAL=true
#VERIFY_HTTPS=true
#CLOCK=30
#REFRESH_CYCLE=1440

# Optional: Technitium PTR Catalog Settings
#TECHNITIUM_PTR_CATALOG=cluster-catalog.dns.local
#TECHNITIUM_USE_SOA_SERIAL_DATE_SCHEME=true
#TECHNITIUM_IPV6_PTR_PREFIXLEN=64
#TECHNITIUM_ZONE_CREATE_FALLBACK_NO_CATALOG=true
#TECHNITIUM_PTR_ONLY_OVERWRITE=false

# WireGuard Support
# Enable WireGuard client DNS synchronization
#ENABLE_WIREGUARD_DNS=false
# Map WireGuard instance names to DNS zones: "instance_name=dns.zone,another=other.zone"
#WG_INSTANCES_DNSZONES=wg1=vpn-wg1.example1.com

# Debug Mode
# Enable detailed debug logging (shows add/delete/update operations, sync modes, etc.)
#DEBUG_MODE=false

